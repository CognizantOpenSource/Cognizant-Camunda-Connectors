{
  "authentication": {
    "type": "clientSecretAuth",
    "clientId": "{{secrets.clientId}}",
    "tenantId": "{{secrets.tenantId}}",
    "clientSecretId": "{{secrets.clientSecretId}}",
    "fullyQualifiedNamespace": "{{secrets.fullyQualifiedNamespace}}",
    "connectionString": "{{secrets.Connection_String}}"
  },
  "inData": {
    "consumer": {
      "consumerOperationDefinition": "receiveMessages",
      "peekNumMaxMessages": 1,
      "queueName": "dummy",
      "pollingWaitTime": 3,
      "serviceBusType": "queue"
    }
  }
}
